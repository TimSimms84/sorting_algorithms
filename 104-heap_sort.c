#include "sort.h"

/**
 * heap_sort - heap sort on array
 * @array: array
 * @size: size of array
 */

void heap_sort(__attribute__ ((unused)) int *array, size_t size)
{
	size = 0;
	printf("%i", size);
}
